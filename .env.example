# Rename to .env and fill in before running docker compose

# GitHub runner configuration (official docs: https://docs.github.com/en/actions/hosting-your-own-runners/about-self-hosted-runners)
RUNNER_NAME=lan-runner-01
RUNNER_LABELS=lan,cpu,intel
# Use a short-lived registration token from repo/org Settings > Actions > Runners > New runner
RUNNER_TOKEN=
# Set either REPO_URL or ORG_URL (not both). Example: https://github.com/your-user/your-repo
REPO_URL=
# Example for org-wide: https://github.com/your-org
ORG_URL=
RUNNER_WORKDIR=/actions

# Ollama configuration
OLLAMA_HOST_PORT=11434
OLLAMA_GPU= # leave empty for CPU-only; set to "all" if GPU present and NVIDIA container toolkit installed
OLLAMA_MODELS_DIR=./ollama_data

# Optional: Proxy
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=localhost,127.0.0.1
